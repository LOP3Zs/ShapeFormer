# Core ML libraries (Vast.ai usually has PyTorch pre-installed)
tensorboard
cmake
onnx

# Facebook Research libraries
git+https://github.com/facebookresearch/fvcore

# Computer Vision & Object Detection
pycocotools
ninja
yacs
cython
matplotlib
tqdm
shapely

# OpenCV (exact version from Dockerfile)
opencv-python==4.4.0.40

# Machine Learning libraries
scikit-learn
scikit-image

# Model libraries (exact version from Dockerfile)
timm==0.4.12

# Setup tools (exact version from Dockerfile)
setuptools==59.5.0

# Logging & Visualization
tensorboardX

# Note: PyTorch (torch, torchvision, torchaudio) usually pre-installed on Vast.ai
# Note: Install these separately after requirements.txt:
# detectron2 - requires CUDA-specific installation
# aistron - custom library installation